/*
 * This file is for the ajax response for the Broker Login form
 */

  // Form submitted successfully, close the modal and show success alert
  console.log("made it here ")

  var successDiv = $("#.overlay-close");
  successDiv.removeClass('.overlay-close');

  // Reload brokers list partial
  var brokerListDiv = $('#overlay-search');
  brokerListDiv.html("<%= escape_javascript(render partial: "show", locals: {}) %>");

  var filterModalDiv = $('#filter_modal');

  filterModalDiv.on("click", '.modalToggler', function(e) {
      if (e.originalEvent.isTrusted) { // Click event triggered by mouse
              // Display modal
              $('#modal').modal('show');
          }

  });
